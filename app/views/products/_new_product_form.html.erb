<%= simple_form_for @product, remote: true do |f| %>

  <%= f.input :name %>
  <%= f.input :price %>
  <%= f.input :permalink %>

  <%= f.submit 'Submit', class: 'btn btn-primary btn-block' %>

<% end %>
